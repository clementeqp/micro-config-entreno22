
server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: Config-server
          uri: https://github.com/clementeqp/micro-config-entreno22
          search-paths: config-data
  application:
    name: config-srv

